# File analyze.mac
#
# Trigger level, consider wave < 0
trgLevel = -0.050
#
# Baseline evaluated in [lowBase, upBase]:
lowBase = 100
upBase = 300
#
# Charge and amplitude evaluated in [lowInt, upInt]:
lowInt = 400
upInt = 1000
#
# Number of circles for time and position estimation
nCircles_Time = 1
nCircles_Position = 1